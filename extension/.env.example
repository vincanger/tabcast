# Copy to .env to build the extension against your own deployment.
# Without these the build falls back to `wasp start` defaults, and either way
# both URLs stay editable under "Server settings" in the popup.

WXT_SERVER_URL=http://localhost:3001
WXT_DASHBOARD_URL=http://localhost:3000
